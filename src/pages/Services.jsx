import React from 'react';
import PagePlaceholder from './PagePlaceholder';

export default function Services() {
  return (
    <PagePlaceholder
      title="Services"
      note="The full editorial overview of all six divisions lands here in Phase 5b. Each division already resolves to its own /services/:slug page below."
    />
  );
}
