import React from 'react';
import PagePlaceholder from './PagePlaceholder';

export default function Contact() {
  return (
    <PagePlaceholder
      title="Contact"
      note="The full two-column contact form (validation, honeypot, success/error states) and office details land here in Phase 5f. This route replaces the old ConsultationModal as the site's one conversion surface."
    />
  );
}
